import ShippingForm from "@/components/ShippingForm/ShippingForm";

/**
 * The page component that displays the shipping form.
 *
 * @returns {JSX.Element} - The rendered page component.
 */
export default function page() {
    return <ShippingForm />;
}
